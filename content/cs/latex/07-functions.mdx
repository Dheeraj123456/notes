---
title: Functions
description: Standard function names, trigonometric functions, logarithms, and custom operators in LaTeX
---

# Functions

Function names should be in roman font, not italics:

- Correct: $\tan(at-n\pi)$ — `\tan(at-n\pi)`
- Incorrect: $tan(at-n\pi)$ — `tan(at-n\pi)`

## Trigonometric

| Render | Code | Render | Code |
|--------|------|--------|------|
| $\sin$ | `\sin` | $\cos$ | `\cos` |
| $\tan$ | `\tan` | $\cot$ | `\cot$ |
| $\sec$ | `\sec` | $\csc$ | `\csc` |
| $\arcsin$ | `\arcsin` | $\arccos$ | `\arccos$ |
| $\arctan$ | `\arctan` | $\sinh$ | `\sinh` |
| $\cosh$ | `\cosh` | $\tanh$ | `\tanh$ |
| $\coth$ | `\coth$ | $\deg$ | `\deg` |

## Logarithms and Others

| Render | Code | Render | Code |
|--------|------|--------|------|
| $\log$ | `\log` | $\ln$ | `\ln$ |
| $\lg$ | `\lg$ | $\exp$ | `\exp` |
| $\max$ | `\max` | $\min$ | `\min$ |
| $\lim$ | `\lim` | $\limsup$ | `\limsup$ |
| $\liminf$ | `\liminf$ | $\sup$ | `\sup` |
| $\inf$ | `\inf` | $\Pr$ | `\Pr` |
| $\det$ | `\det` | $\dim$ | `\dim` |
| $\hom$ | `\hom` | $\ker$ | `\ker$ |

## Custom Operators

```latex
\operatorname{f}(x)
```

$$
\operatorname{f}(x)
$$

| Render | Code |
|--------|------|
| $\argmax$ | `\argmax$ |
| $\argmin$ | `\argmin$ |
| $\plim$ | `\plim$ |

---

[← Back: Variable Size Symbols](/cs/latex/06-variable-symbols/)  
[Next → Logic and Set Theory](/cs/latex/08-logic-sets/)
