---
title: Logic and Set Theory
description: Quantifiers, set operations, logical connectives, and implication symbols in LaTeX
---

# Logic and Set Theory

## Quantifiers

| Render | Code | Render | Code |
|--------|------|--------|------|
| $\forall$ | `\forall` | $\exists$ | `\exists$ |
| $\nexists$ | `\nexists$ | $\exist$ | `\exist` |

## Set Operations

| Render | Code | Render | Code |
|--------|------|--------|------|
| $\in$ | `\in` | $\notin$ | `\notin$ |
| $\ni$ | `\ni` | $\subset$ | `\subset` |
| $\supset$ | `\supset$ | $\mid$ | `\mid` |
| $\emptyset$ | `\emptyset$ | $\varnothing$ | `\varnothing$ |
| $\complement$ | `\complement$ | $\cap$ | `\cap` |

## Logical Connectives

| Render | Code | Render | Code |
|--------|------|--------|------|
| $\land$ | `\land$ | $\lor$ | `\lor` |
| $\lnot$ | `\lnot$ | $\neg$ | `\neg` |

## Implication and Relations

| Render | Code |
|--------|------|
| $\to$ | `\to$ |
| $\gets$ | `\gets` |
| $\leftrightarrow$ | `\leftrightarrow$ |
| $\mapsto$ | `\mapsto$ |
| $\implies$ | `\implies$ |
| $\impliedby$ | `\impliedby$ |
| $\iff$ | `\iff` |
| $\therefore$ | `\therefore$ |
| $\because$ | `\because$ |

## Sets

$$
\Set{ x \mid x<\frac 1 2 }
$$

```latex
\Set{ x \mid x<\frac 1 2 }
```

---

[← Back: Functions](/cs/latex/07-functions/)
